# Copy to `.env` (gitignored). See README.md in this folder.

# Connect to Postgres (leave unset to use in-memory data for local dev only).
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# If the frontend uses Supabase Auth, set the JWT secret from Supabase → Project Settings → API.
# SUPABASE_JWT_SECRET=
