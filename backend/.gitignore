# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.pyc
.pytest_cache/
*.egg-info/

# Secrets (commit `.env.example`, not real `.env`)
.env
.env.local
.env.example
# R / renv (if present elsewhere in repo)
renv/
