/**
 * Mock data removed - using real patient data from API
 * Patients are now fetched from: GET /record-officer/patients
 * This export is kept empty for backward compatibility
 * @deprecated Use API calls instead of this mock data
 */
export const patientsList = [];
